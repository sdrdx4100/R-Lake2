# R-Lake2

## Structure

- `/backend`: Django + Django REST Framework API
- `/frontend`: React + TypeScript + Vite SPA

## Development

- Frontend: `npm install && npm run dev`
- Backend: create a virtual environment, install `/backend/requirements.txt`, then run `python manage.py runserver` inside `/backend`
